{
  "name": "mcp-postgres-server",
  "version": "1.0.0",
  "description": "MCP Server for PostgreSQL database access with SELECT-only queries. Reads connection string from MCP_PG_CONNSTR in the project's .env file.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "keywords": [
    "mcp",
    "postgresql",
    "database",
    "model-context-protocol"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "dotenv": "^16.4.5",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
